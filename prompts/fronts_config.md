# Config

## I have started a module config.py in fronts/finding/ and wish for you to finish it.  It should load in a YAML file and return a dict.  Please generate a sample YAML file in fronts/dev named finding_A.yaml with fields based on the finding_dmodel dict in config.py

## I have modified the config file to include nested parameters. An example is finding_config_A.yaml in fronts/finding/configs.  I have modified the data model in fronts/finding/config.py to be nested too.  Can you modify the load() method in config.py to allow for this nested structure?  Also, generate a test module named test_finding_config.py in tests/ to test.  If you need to run python, use the "ocean14" environment in conda.